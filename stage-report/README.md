# stage-report · 索引

視網膜血管分割（以及更廣義的曲線結構分割）的一系列實驗。
E0 到 E18 全部在一台六核筆電上以 CPU 跑完，沒有 GPU；
E13b 起改在實驗室的兩張 RTX 4070 Ti 上跑。

閱讀順序：**stage_0 → stage_1 → E0 → E2 → E3'/E1' → E6 → E4 → E5 → E7 → E8 → E9 → E9b → E10 → E11 → E12 → E14 → E15 → E16 → E17 → E13 → E13b → E18**。

E0 到 E6 都是量測，E7 是第一個動模型的實驗。

---

## 背景兩篇

| 檔案 | 內容 |
|---|---|
| [stage_0](stage_0.md) | 第一輪的七個發現：Dice/clDice/β₀ 的量測、連通塊過濾的效果、對比 vs 粗細的混淆變項、STARE 兩位標註者的差距 |
| [stage_1](stage_1.md) | 四個定位問題（領域下一步、為何不用大模型、是不是都在打 benchmark、能做什麼），以及**初版五處被文獻推翻的說法** |

---

## 實驗報告

| 實驗 | 問題 | 一句話結果 |
|---|---|---|
| [E0](e0_literature_check.md) | 我們規劃的實驗撞題了嗎 | 兩個「撞題」都是假的。拓樸不確定度那篇從未用真實標註者分歧驗證；多標註者那條線一個拓樸指標都沒報過 |
| [E2](e2_contrast_stratified.md) | 全圖平均藏了什麼 | 全圖 Dice 說四種 loss 差 0.0002；分層後是 +0.0213（t=9.7）與 −0.0070（t=−8.9）**兩個顯著且相反的效果互相抵消** |
| [E3'/E1'](e3_e1_annotator_topology.md) | 換標註者對拓樸的影響；模型會在人類吵架的地方猶豫嗎 | 斷點數**可以靠多預測買到**，跨標註者比較是混淆的。逐像素不確定度在爭議最密集的一層**反向失效**（AUROC 0.373）|
| [E6](e6_structure_uncertainty.md) | 換成結構層級會不會就成立 | 會，Q1 從 0.388 升到 0.701（120/120 張圖）。但對照組顯示**功勞屬於「沿曲線聚合」，不屬於「在交叉點切開」** |
| [E4](e4_cross_dataset.md) | 「過濾贏過拓樸 loss」是 DRIVE 特有的嗎 | 不是，HRF 2.41×、TopoMortar 2.58×。**但只在 β₀ 成立**。兩次修訂：「只在 β₀」是構造上的必然；而 β₁ 的「loss 贏過濾 15 倍」被 E9 推翻，是拿錯工具比 |
| [E5](e5_label_noise.md) | 拓樸 loss 是否更容易被標註噪聲帶壞 | **是 cbDice，不是 clDice**。但主要收穫是方法學：逐影像配對檢定在 700 對上給出 p=3×10⁻⁴，而那個差值在僅有的兩顆種子之間**符號翻轉**。六次翻轉有五次在 `accurate` 組。β₁ 在填洞對照後**任何標註條件下都沒有 loss 贏** |
| [E7](e7_contrast_gated.md) | 把拓樸壓力調到該付代價的地方，模型會變好嗎 | **事前判準三條中兩條不成立**，三顆種子全部同號。訊號來源確實重要（t = −7.32），**但方向相反**：控制組 `G_focal`（模型端信心閘）贏假設組 0.0177、贏 clDice 基線 +0.0136。E1' 的量測沒錯，**我把一個診斷當成了禁令**。但用 E10 的 `severs` 重讀：G_focal 買到的是**中心線覆蓋率，不是連通性** |
| [E8](e8_ensemble_uncertainty.md) | 換一個不確定度訊號能不能修好 E1' 的反轉 | 種子分歧沒用（Q1 0.386→0.380）；改用**不同標註者**訓練的成員後 Q2/Q3 大勝（40/40 格），**但 Q1 仍是 0.390、仍然反向**。四種讀出方法都在同一層失效，且模型對爭議像素比對無爭議像素更有把握 → **要修最暗的一層只能改模型學到什麼** |
| [E9](e9_hole_sweep.md) | 填補小洞救得回迴路嗎（β₁ 的對偶後處理） | 分資料集：兩個視網膜資料集**更差**（DRIVE 29.5→41.3，**240/240 條曲線**；HRF 46.1→56.3），這構成證明——模型的迴路本來就太少；TopoMortar **砍 55%**，且 loss 之間的差距一格從 15.3 塌到 0.4。**推翻 E4 結果二**——每個拓樸指標都有對應的後處理操作，要先排除才能宣稱。（其結論後來又被 E9b 修正，見下列）|
| [E10](e10_break_anatomy.md) | 「斷口」到底是什麼 | **只有 7% 真的切斷連通性**，93% 兩側是同一塊。直接分類與 β₀ 誤差相關 0.456，原始斷點數只有 0.095。後果：斷點數在量覆蓋率不是拓樸；而 clDice 的優勢被低估（原始 4.9% → 真切斷 10.9%）|
| [E9b](e9b_bridge_sweep.md) | 焊接斷口能不能生出迴路（E9 自己要求的對照） | **能，E9 錯了**。扣掉閉運算的填洞副作用後 DRIVE β₁ 29.5→24.9，223/240 條曲線變好，且同時把 β₀ 從 22.1 降到 16.6、Dice 只付 0.002。HRF 複製（補回 30%，61/90）。修正版：**70–91% 的迴路缺口才是模型端問題**（視願付的 Dice 而定）|
| [E11](e11_bridge_anatomy.md) | 焊接到底修好了什麼 | 三個事前預測，兩個中、一個錯。**焊接的 β₀ 好處一比一等於被解決的真切斷數**（比值 0.89–0.99），這是 E10 指標的第二次獨立驗證。我自己的「血管太細才是天花板」被推翻（8.4 點對長度的 38.3 點），E9b 的長度解釋反而被平反。意外收穫：**54.7% 的修復發生在別的位置** |
| [E12](e12_confidence_gate.md) | E7 控制組的勝利撐得住新種子嗎 | **撐住了，而且變大**：Dice 增益從 +0.0136 升到 **+0.0184**（t=10.93，六顆全正），新三顆種子彼此差不到 0.003——E7 的離散是舊種子造成的。但判準 3 **往反方向不成立**：真切斷**多了 1.85 個**（六顆中五顆為正）。**`G_focal` 是中心線召回率的方法，帶連通性代價**，不是拓樸方法 |
| [E14](e14_augmentation.md) | 改變「給模型看什麼」會不會比改 loss 有用 | **會，而且是本系列最大的一次介入。** 查文獻時發現我們的管線完全沒有資料增強。補上之後：五個指標、三顆種子，`H_aug` 每一顆都贏基線每一顆（β₀ −30%、HD95 −16%）。**判準 2 是本系列第一次有東西通過「真切斷」那條線**，六顆種子上收斂得更緊（−3.2，全距 1.65）——而且它不動 loss，動的是輸入。判準 1（最暗層 Dice）**六顆種子仍不過**，五正一負，擋下它的是 −0.0021。真正穩的是 **Q2：十二顆種子全正，最小 +0.0188**（探索性）|
| [E15](e15_expected_run_length.md) | 換成領域標準的拓樸指標會怎樣 | **E7 的結論被改寫**。ERL（能無誤差追蹤多遠）下，G_focal 比基線多 **+573.9 px**（三顆種子全同號，可追蹤比例 26.1% → 32.6%）。分層後：**Q3/Q4 已在天花板的 99%，所有空間都在 Q1（18%）**。08-23 補記：六顆種子確認（+534.0，t=7.49，新種子效果更大），而且 **`H_aug` 與 `G_focal` 打平（分不出來）**——同樣的可追蹤長度，一個用連通性換，一個順便把連通性修好 |
| [E16](e16_liot.md) | 在輸入端刪掉對比，會不會勝過教模型看穿對比 | **第一版判定是量測 bug**（最亮層 −0.95、t=−315 是「什麼都沒預測」，不是「比較差」）——`predict_full` 從模型讀「要不要編碼」，但「用哪組常數」還留在呼叫端。**最危險的是那個 [PASS]**：什麼都不預測的模型真切斷數趨近零，而事前寫的解讀表正確擋下了它。修好後：DRIVE 內部 LIOT 每層都略輸 `H_aug`（判準 1 −0.0088），**但零樣本 HRF 上是全表最好**。**同一批檢查點，換資料集結論翻過來** |
| [E17](e17_transfer.md) | 換一台相機，這些結論還在嗎 | **資料增強的優勢在轉移上是四到六倍**：DRIVE 內部 +0.0106，STARE +0.0476、HRF +0.0678，六顆種子全正——**單一資料集的比較會系統性低估它**。LIOT 在 HRF 上是全表最好的 Dice、最少真切斷、最高可追蹤比例，但判準 B **沒過種子閘**。**三次翻符號全部是 seed 1。** 最刺眼的數字：HRF 上最好的 ERL 只有天花板的 **5.6%**——Dice 還有 0.73，拓樸完全崩掉。**2026-08-25 修訂**：補進 w32 與 `K_focal_aug`。K 對 H_aug 的 **β₀ 優勢兩個資料集都過閘**（STARE −24.7、HRF −104.3），但 **E18 在 DRIVE 上最強的 ERL 訊號（t=13.41）在轉移上 t=0.63／0.57，不見了**——K 帶得走「少切成幾塊」，帶不走「每段有多長」 |
| [E13](e13_capacity.md) | 這些結論是不是「小模型」的結論 | **不是，而且小模型低估了它們。** 4 倍寬度後，增強對基線的 Q1 差距從 +0.0185 **長到 +0.0399**（過閘），拓樸損失對基線的差距從 +0.0186 **塌成 −0.0040**（不過閘）。base=16 時增強與 clDice **打平**（−0.0002），base=32 變成增強單方面贏 **+0.0439**（t=10.52）。**E5 的「loss 沒差」升級成「那點差別本身就是小模型效應」。** 轉移上獨立佐證：沒有增強時加寬度讓 β₀ **變差**（四格全同向），有增強時變好 |
| [E13b](e13b_capacity_31m.md) | 那個趨勢在真實模型尺寸上還成立嗎 | **成立，但在減速，而且重訓翻掉了 E13 的一格判定。** 第三個點是 5 層 base=64 的 **31,042,369** 參數（不是加寬的 3 層）。增強對基線的 Q1 差距 +0.0116 → +0.0415 → **+0.0523**（五顆種子全同號，過閘），兩段增幅 +0.0298 / +0.0108 —— 後者用了 64 倍參數只買到三分之一的增幅。「增強勝過拓樸損失」在 467k 之後**完全平了**（+0.0625 → +0.0633）。拓樸損失對基線在**三個容量全部未過閘**。⚠️ 前兩點的 27 個 checkpoint 重訓後，**base=16 的差距 2 從「+0.0186 t=12.55 六顆同號過閘」變成「+0.0021 t=1.53 三正三負」** —— base=32 三格全部重現，所以不是移植 bug，是那個效應小到另一組六次訓練重現不出來 |
| [E18](e18_gate_plus_augmentation.md) | 兩個有效的東西合起來會怎樣 | **本系列最大的一次改善。** 信心閘 clDice + 資料增強：可追蹤長度（ERL）從基線的 **26.4% 拉到 44.7%**（三顆種子全同號，+1653 px），β₀ 誤差對半（89.7→35.8）。分層 Dice 看起來很糟（Q2 −0.054、Q3 −0.024），**我一度據此判定推薦不成立——那是錯的**：Q2 的 Dice 掉了但 Q2 的可追蹤比例是全表最高。E15 事先立的 ERL 仲裁規矩生效。**判準的漏洞是真的**：四條只保護 Q1/連通性/Q4，損害全落在沒人看的 Q2/Q3 |

---

## 這一系列真正學到的東西

九件比任何單一數字都重要的事：

**一、單一平均指標會系統性地隱藏方法差異。**
E2 在 DRIVE 上、E4 在 TopoMortar 上、E6 在不確定度上，
三次看到同一個模式：一個總數把兩個方向相反的效應平均掉。
分層報告的成本是零。

第四次出現在 E9b 自己身上，而且是當天早上寫的報告：
HRF 的整體迴路缺口是 −40.7，看起來焊接理所當然該有效。
拆開來是**健康影像 +1.3、病變影像 −56 到 −67**——
健康視網膜的模型根本沒有缺口，焊接在那裡是有害的（4/5 張變差，p = 0.017）。
修正後的說法：**有缺口時焊接有效（三個資料集都複製，−4.7 到 −13.6），迴路數接近正確時有害，過多時中性**。
這條規則是**逐影像**的：TopoMortar 整體方向相反，它自己的 89 個深缺口單位仍然改善 −6.8。

**二、加一個控制變項，經常會翻掉原本的解讀。**
本系列有五次自我推翻：
stage 0 的「clDice 免費獲勝」被連通塊過濾推翻；
E3' 的「用 vk 訓練拓樸較好」被骨架精度推翻；
E6 的「結構層級救回了暗處」被等大對照組推翻；
E4 的「β₁ 上 loss 贏過濾 15 倍」被 E9 的填洞掃描推翻；
E9 的「β₁ 只能從模型端修」被 E9b 的焊接掃描推翻——**同一天內推翻自己**。
每一次都是加一個對照，不是換一個模型。

第四、五次合起來給了一條可操作的規則，而且第五次示範了它多容易套錯：
**每一個拓樸指標的每一個方向都有對應的後處理操作，要先排除，宣稱才站得住。**

| | 太多 | 太少 |
|---|---|---|
| β₀（連通塊） | 移除小連通塊 | 焊接斷口 |
| β₁（迴路） | 填補小洞 | 焊接斷口 |

E4 只掃了左上一格就對兩個指標下結論；
E9 補了左下一格，卻把「移除型不行」推廣成「後處理都不行」；
E9b 補上右邊那一欄，推翻 E9。**規則寫對了，套用時漏掉一半的操作空間。**

**三、模型端的訊號會一起瞎。**
E1' 量到逐像素信心在最暗的一層反向失效（AUROC 0.373）。
E8 換了三種讀出方法——平均、種子分歧、不同標註者訓練的模型之間的分歧——
在 Q2 以上都有效，**在最暗那一層全部仍然反向**。
換讀出方法救不了，只能改模型學到什麼。

**四、統計單位是「訓練」，不是「影像」。**
E5 用 700 個 (影像, 種子) 配對算出 p = 3×10⁻⁴，
而那個差值在僅有的兩顆種子之間符號相反（−0.0271 / +0.0102）。
700 對來自 **2 次訓練**；配對檢定量到的是「同一個模型在不同影像上表現一致」，
不是「這個 loss 比較好」。這叫**偽重複**，而且 √n 會放大它：
同樣的種子間偏移在 n=60 只給 |t| = 0.99，在 n=700 給 |t| = 11.9。
**n 愈大，錯的方法愈自信。**
配對仍然值得做（它移掉影像難度的變異），但不能單獨成立——
現在的規矩是配對檢定「且」每顆種子符號一致，
已寫進 `summarize_gated.py` 的事前判準，在 E7 結果出來前裝上。

**五、「量到的性質」和「介入的效果」是兩回事。**
E1' 量到模型對自己漏掉的暗血管很有信心（AUROC 0.373）。那個量測完全正確，而我把它當成了「別用信心當訓練訊號」的禁令，寫進 E7 的整個假設。
錯在：那是一個描述**收斂後**模型的量測，不能用來預測一個在**每一步**改變優化路徑的介入。
診斷描述的正是那個訊號要去修的病。
附帶的一條：**控制組要當成真的實驗來設計**——`G_focal` 只是為了讓判準 3 有東西可比才存在，現在它是本系列最好的模型端結果。

**六、一個看起來完整的結果檔，不代表它讀到了完整的資料。**
E12 訓練了種子 3–5，`stratify.py` 和 `break_lengths.py` 各自把種子範圍
**手寫成 `(0, 1, 2)`**，於是它們算的是舊的那三顆。
`summarize_confidence.py` 照樣印出一份格式完整的判定，
而那份判定**開啟了下一個實驗的佇列閘**。
沒有任何一步報錯，檔案是新的，內容是錯的。

同樣的手寫種子範圍在 repo 裡有**五份拷貝**
（`stratify` / `break_lengths` / `hole_sweep` / `breaks` / `speckle`），
所以這不是一次筆誤，是一個會被複製的形狀。
修法是 `train.trained_runs()`——去掃磁碟，而不是宣告一個範圍；
檢查寫在 `test_trained_runs.py`，斷言的正是失敗的那條性質：
**一個 seed 9 的目錄要被找到，一個只有 `ckpt.pt` 的半成品不能被算進去。**

推而廣之：**閘門要擋在「數字是對的」上，不是擋在「檔案存在」上。**
CLAUDE.md 早就寫了「絕不等 PID，要等成品」——這次成品有了，成品是錯的。

**七、一個決定只搬一半，比完全不搬更危險。**
E16 把「要不要把輸入編碼成 LIOT」從呼叫端搬進 `predict_full`
（讓它從模型自己讀），而且我在報告裡把這寫成優點。
但「用哪組正規化常數」留在呼叫端沒搬。

於是三支分析程式**正確地編碼，然後用灰階常數正規化 0–255 的位元組**——
輸入落在 800 個標準差之外，模型什麼都不輸出，
判定印出 Dice 0.0000 當成一項關於 LIOT 的發現。
`transfer.py` 沒中，只因為我在那裡另外寫了一份常數邏輯：
**同一個決定在 repo 裡有兩份實作，一份對一份錯。**

如果兩半都留在呼叫端，呼叫端至少會看見它們並列而想起要一起改。
搬走一半之後，剩下的那一半看起來就只是個普通參數。

修法是 `train.normalisation()`（兩件事一起決定，一次），
加上 `predict_full` 的守衛：**4 通道模型收到純量常數就丟例外**。
那一行讓安靜的錯誤變成大聲的錯誤，本來就該存在。

推而廣之：**把決定搬進函式時，要搬走的是「整個決定」，不是它的一半。**
搬完之後問一句：呼叫端還剩什麼參數是必須和我剛搬走的東西一致的？

**八、判準要蓋滿所有可能被犧牲的維度，不是我猜的那一個。**
兩次犯同一類錯：
E14 的判準 1 押在 Q1，因為我預期效果在那裡——結果 Q1 是種子變異最大的一層，判準倒在那裡。
E18 的判準 4 保護 Q4，因為 E15 說那層在天花板、只會賠不會賺——
結果賠在 **Q2（−0.0539）和 Q3（−0.0237）**，兩層都沒有任何判準看著。
Q4 賠 0.0030 被抓到並判 FAIL，Q2 賠的是它的十八倍，完全隱形。

E18 那次結局是好的（ERL 顯示 Q2 的 Dice 損失是良性的，那些像素反而連得更好），
**但那是運氣。** 如果損失是真的，那套判準會印出「三條通過、推薦採用」而看不見它。

分層報告的成本是零（第一條），所以沒有理由只保護一層。
**規矩：分層指標的事前判準要每一層都有一條，非劣性也算。**

**九、先查文獻再設計實驗。**
stage_1 初版規劃的五個實驗有兩個撞到既有工作，
直接開跑會浪費約 22 小時運算並得到沒有貢獻的結果。
但 E0 也補上後半句：**查到撞題不要直接放棄，要讀進實驗設定確認撞的是哪一塊**——
那兩篇在摘要層級看起來重複，在實驗設定層級其實互補。

E7 補上第三句，而且是反方向的：**查到「沒人做過」的時候，要確認自己列舉的維度是完整的。**
E7 把設計空間寫成「權重來自影像端 vs 模型端」兩格，宣告沒人做過影像端。
那句話沒錯，但 2026-08-24 重查時發現 Smooth clDice 佔著**第三格：幾何端**
（權重來自血管邊界周圍的空間不確定帶）。
那個維度真正的名字是「權重從哪個來源推導」，它至少有四個值：
**影像、模型、標籤、幾何**。列兩格就宣告涵蓋，是把「我想到的」當成「存在的」。

---

## 程式

全部在 [`../exp/`](../exp/)。資料由 `fetch_*.py` 下載，不進版控。
每個實驗的產出檔案列在各自報告的「產出的檔案」一節。
